{% extends '@EasyAdmin/page/content.html.twig' %}

{% block content %}
    <div style="text-align: center; margin-top: 50px;">
        <h1>{{ title }}</h1>
        <p>{{ subtitle }}</p>
        <p style="margin-top: 20px;">Możesz zarządzać użytkownikami, lokalizacjami i wiadomościami za pomocą menu po lewej stronie.</p>
    </div>
{% endblock %}
