<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Logowanie</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="stylesheet" href="{{ asset('bundles/easyadmin/app.16e44df6.css') }}">
    <style>
        body, html {
            height: 100%;
            margin: 0;
        }
        .container {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100%;
        }
        .ea-login-container {
            max-width: 400px;
            width: 100%;
            padding: 30px;
            background-color: white;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
        }
        .ea-login-form .form-control {
            width: 100%;
        }
        .text-center {
            text-align: center;
        }
    </style>
</head>
<body class="ea-light">
<div class="container">
<div class="ea-login-container">
    <h1 class="text-center">Panel administracyjny</h1>

    <form method="post" action="{{ path('admin_login_check') }}" class="ea-login-form">
        <div class="form-group">
            <label for="username">E-mail</label>
            <input type="text" id="username" name="_username" class="form-control" required autofocus>
        </div>

        <div class="form-group">
            <label for="password">Hasło</label>
            <input type="password" id="password" name="_password" class="form-control" required>
        </div>
        <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">


        <button type="submit" class="btn btn-primary btn-block">Zaloguj się</button>

        <p class="text-center" style="margin-top: 20px;">
            <small>Nie pamiętasz hasła? Skontaktuj się z administratorem.</small>
        </p>
    </form>
</div>
</div>
</body>
</html>
